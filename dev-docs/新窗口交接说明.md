# agent蜂群新窗口交接说明

## 当前快照

- 项目：`F:\projects\agent-swarm`
- 当前工作口径：阶段 0-37 均已完工。阶段 25 真实模型、阶段 26-29 审批链、阶段 30-33 审查/gate/dry-run/lock、阶段 34 极小真实 Runner 已开放、阶段 35 受控模型目录已开放、阶段 36 全自动模式宪法与权限边界方案已完成、阶段 37 想法引导官已完成。34 仅临时沙箱+固定 Git 只读命令+人工确认；不开放批量/自动 Runner、自由命令、Git commit/push。35 不开放前端自由模型配置、key、base URL。36 不开放自动 Runner 执行、不自动写文件、不自动改 Git。
- 当前阶段：阶段 38 准备开始，目标是“项目类型分流与通用想法入口”。首页输入一句想法后，系统先判断项目类型，再进入对应想法引导模板。旧“多 Provider Adapter”顺延到阶段 39。
- 当前最新 commit：以 `git log -1 --oneline` 为准；不要在交接文档里手工认定旧 commit 为最新状态。
- 已完成 MVP-0.3 闭环：项目计划审批 -> Agent 自动分工 -> 只读 Runner request queue
- 已完成 MVP-0.4 闭环：execution request 审查视图 -> 生命周期流转 -> runtime events 审计
- 阶段 2 已完成第一小步：`model_calls` 结构草案和真实模型调用记录边界；当时不建表、不写入、不调用 provider。阶段 23 已补上 SQLite 审计表，阶段 2 时仍不写调用记录、不调用 provider（截至阶段 35.1，真实模型调用已通过 Model Gateway 开放）。
- 阶段 2 已完成第二小步：Model Gateway 未来正式入口、provider 配置来源和 Key 存储边界。阶段 2 当时不涉及 provider 请求发送；截至阶段 35.1，真实 provider 请求只能经 Model Gateway、受控模型目录、脱敏审计链路发起，不得绕过。
- 阶段 2 已完成第三小步：provider config resolver helper、redaction / response limiter helper、安全 `model_calls` 记录草稿，以及禁用态 `project-plan-model-requests` route 草案验证。
- 阶段 2 已完成第四小步：helper-only `model_calls` 写入 / 迁移草案已落地；阶段 2 已收口。阶段 23 已在新 Tauri/Rust 架构中建立 `model_calls` SQLite 审计表。阶段 23 当时不写入业务记录、不调用 provider（截至阶段 35.1，真实模型调用已通过 Model Gateway 开放，model_calls 审计已落库）。
- 阶段 3 已完成：Agent Run 本地记录链、失败注入、`agent_runs` Mock / SQLite API、runtime event 审计、Web UI 记录页和回归验证已落地。阶段 3 的 Agent Run 记录链定位为本地 Mock/SQLite 审计视图，不涉及真实 Agent 触发、真实模型调用或 Runner 执行（真实模型已在后续阶段 25/35 开放，Runner 已在后续阶段 34 边界内开放）。
- 技术栈已定：Tauri 2 + Rust + React + TypeScript + Vite + Ant Design + SQLite + rusqlite，桌面端主入口，Web 仅辅助预览入口。
- 目录架构已确认：`packages/ui` 作为唯一 UI 源，`packages/agent-core`、`packages/shared` 作为共享层，`apps/desktop` 作为主入口，`apps/web` 作为辅助预览入口。
- 共享 UI 已起步：`packages/ui` 已建立 React + TypeScript + Vite + Ant Design 最小骨架，`OverviewPage` 已接入项目、Agent、Task、Approval 的只读数据，可类型检查和生产构建。
- Tauri/Rust 已起步：`apps/desktop` 已初始化最小宿主，`get_project`、`list_agents`、`list_tasks`、`list_approvals` 已通过 Tauri invoke 从 SQLite 读取只读数据；SQLite 运行库放 Tauri app data 目录，不写入 `data/local/`。
- 只读骨架正式验收文档已新增：`docs/tauri-readonly-skeleton-acceptance.md`。
- 教程 #11 写入安全边界裁剪文档已新增：`docs/write-commands-security-design.md`。
- 写入 commands 设计文档已新增：`docs/write-commands-design.md`。
- 写入 commands Rust 实现和正式验收已完成：`create_task`、`update_task_status`、`create_approval`、`approve_approval`、`reject_approval`、`patch_only_approval` 均已接入 Tauri invoke，验收见 `docs/write-commands-acceptance.md`。
- 前端共享 UI 写入接入和真实 Tauri 桌面写入交互验收已完成：`OverviewPage` 已接入任务创建、任务状态流转、审批通过 / 拒绝 / 仅补丁；验收见 `docs/frontend-write-commands-design.md` 和 `docs/tauri-desktop-write-interaction-acceptance.md`。
- 阶段 15 旧原型页面迁移清单已完成：旧 `apps/web/` 的 13 个页面、`design/index.html` 的 8 个设计页和旧 `services/api/` 路由已按迁移 / 冻结 / 暂不做分类；见 `dev-docs/旧原型页面迁移清单.md`。
- 阶段 16 独立页面拆分已完成：`OverviewPage` 已精简为总览仪表盘，`TasksPage`、`AgentsPage`、`ApprovalsPage`、`SettingsPage` 已拆出，侧边栏导航支持多页面切换，数据由 `App.tsx` 统一管理；见 `dev-docs/阶段16-独立页面拆分方案.md`。
- 阶段 17 长期分层边界设计已完成：六层边界、调用方向、旧模块冻结条件和阶段 18 最小骨架目标已确认；见 `dev-docs/阶段17-长期分层边界设计.md`。
- 阶段 18 共享类型与规则骨架已完成：`packages/shared`（跨端类型、DTO、常量）和 `packages/agent-core`（状态转换、终态判断、领域规则纯函数）已建纯 TypeScript 骨架；`packages/ui` 通过 tsconfig paths（`@agent-swarm/shared`、`@agent-swarm/agent-core`）从两个新包导入，不再本地定义跨端类型和状态转换规则；未新增 npm 第三方依赖，未建 monorepo workspace。
- 阶段 24 project_plan / Workflow 最小闭环迁移已完成：已补 `project_plan_drafts`、`runner_requests`、显式 `approve_project_plan`、`ProjectPlanPage` 和验收测试。阶段 24 范围内不涉及真实模型调用和 Runner 执行（真实模型已在后续阶段 25/35 开放，Runner 已在后续阶段 34 边界内开放）。
- 阶段 25 真实模型最小可用接入设计已完成：25.1 后端调用链、25.2 前端预览入口、25.3 model_calls 安全审计落库均已实现；见 `dev-docs/阶段25-真实模型最小可用接入设计.md`。真实模型调用可在 Tauri 桌面端人工触发并写入脱敏审计记录；Runner 仍不执行。
- 旧原型归档方案已确认：旧 `apps/web/`、`services/api/`、`design/index.html` 等冻结为参考资产，不直接删除、不继续扩展。

## 这批已经做了什么

- 新增 `services/api/project-plan.js`
- 新增 `POST /api/projects/:projectId/project-plan-requests`
- 工作流页加入项目计划审批入口
- 审批通过后生成五个 queued 任务和五条 `runner_request_readonly` 队列记录
- Mock / SQLite flow、UI smoke、helper 验证都已同步
- execution request 生命周期和 runtime events 审计已在 Mock / SQLite flow 中覆盖
- `docs/data-model-draft.md` 已补入 `model_calls` 草案，`dev-docs/真实模型接入准入规格.md` 已补入模型调用记录边界
- `dev-docs/Model Gateway正式入口设计.md` 已补入候选 route、内部调用链、server env 配置来源和 key 存储边界，并落了禁用态 route 草案
- `services/api/model-gateway-model-calls.js` 已新增 helper-only 的 `model_calls` 写入 / 迁移草案骨架；仍不落盘
- 新增 `services/api/agent-runs.js`
- 新增 `GET /api/projects/:projectId/agent-runs` 和 `POST /api/projects/:projectId/agent-run-requests`
- SQLite seed 会在旧 seed JSON 缺少 `agentRuns` / `runtimeEvents` 时从 Mock 生成物兜底
- Web App 新增 Agent Run 记录页，可创建本地记录链、查看失败注入、token / cost 摘要和审计事件
- `packages/ui` 已从预留 README 升级为可运行 Vite 工程骨架，包含 `src/app`、`src/components`、`src/pages`、`src/layouts`、`src/theme`、`src/i18n`、`src/routes` 和 `src/utils`
- 阶段 23 已完成 model_calls helper-only 迁移。`003_add_model_calls.sql` 已建立 18 字段审计表和 3 个索引；Rust helper-only 草案已补齐，`feature_disabled` 返回不写 `model_calls`、不写 `runtime_events`，70 个 Rust 测试通过。
- 阶段 24 已完成 project_plan / Workflow 最小闭环迁移，见 `dev-docs/阶段24-project-plan-workflow最小闭环迁移设计.md` 和 `docs/project-plan-workflow-acceptance.md`。
- 阶段 25-35 已完成：真实模型调用闭环（调用→审计→保存→审批→实例化）、任务角色模板、只读 Runner request 查看、执行前审查、gate、dry-run、文件范围锁、极小真实 Runner 执行、受控模型目录。阶段 34 Runner 仅临时沙箱+固定 Git 只读命令+人工二次确认；阶段 35 模型选择从后端受控目录获取。

## 现在的边界

- 阶段 34 已开放极小范围真实 Runner（仅临时沙箱+固定 Git 只读命令+人工确认）；不开放批量/自动 Runner
- 阶段 35 已开放受控模型目录：前端可从后端目录中选择模型，后端校验后调用；不开放自由模型配置、key、base URL、prompt、header、body
- 阶段 35.1 已收口前端中文化：用户可见文案默认中文；后端枚举、数据库字段名、内部状态不得直接裸露；英文只允许保留在模型 ID、命令、环境变量、Provider、路径、数据库 ID 等技术标识中
- 真实模型调用已落地（阶段 25.1-25.3，阶段 26）；允许经人工二次确认发起 `project_plan_generation` 预览并写入脱敏审计记录，阶段 26 可保存为待审批草案；不允许自由 prompt 或自动触发
- 不导入 provider SDK；真实 provider 请求只能经 Model Gateway、受控模型目录、脱敏审计链路发起，不得绕过
- 不写入受保护路径，不绕过审批写用户项目文件
- 不改 Git
- 不做云同步
- 不做完整权限系统
- Agent Run 记录链定位为本地 Mock/SQLite 记录和审计视图，不触发真实 Agent、真实 provider 或 Runner（真实模型调用走独立的 project_plan_generation 链路，Runner 执行走阶段 34 闸门链）；Agent Run 记录链不可升级为真实模型调用或真实 Runner 执行通道。
- 阶段 36 已定义全自动模式宪法与权限边界：`manual`/`full_auto` 双模式、L0-L4 权限分级、自动授权矩阵、一键停止和降级规则。全自动模式尚未开放执行，仅定义权限边界和审计规则。旧阶段 36（角色模型绑定）和旧阶段 37（Skill 绑定）已并入新阶段 40。

## 受保护路径

- `design/image2/`
- `_internal/`
- `data/mock/runtime-state.json`
- `data/local/`
- `logs/`
- `.playwright-cli/`

## 先读什么

```text
AGENTS.md
docs/Agent宪法.md
docs/AI开发细则.md
docs/README.md
dev-docs/README.md
dev-docs/应用真正可用落地计划.md
dev-docs/旧原型归档方案.md
dev-docs/真实模型接入准入规格.md
dev-docs/Model Gateway正式入口设计.md
docs/mvp-0.3-project-plan-flow-spec.md
docs/mvp-0.4-execution-layer-plan.md
docs/demo-checklist.md
docs/tauri-readonly-skeleton-acceptance.md
docs/write-commands-security-design.md
docs/write-commands-design.md
docs/write-commands-acceptance.md
docs/frontend-write-commands-design.md
docs/tauri-desktop-write-interaction-acceptance.md
dev-docs/旧原型页面迁移清单.md
dev-docs/阶段16-独立页面拆分方案.md
dev-docs/阶段17-长期分层边界设计.md
scripts/README.md
```

## 推荐继续做什么

- 阶段 37 想法引导官已完成。下一步进入阶段 38（项目类型分流与通用想法入口），先做“一句话想法 -> 项目类型 -> 推荐问题 -> 进入想法引导”的主路径。
- 旧“阶段 38 多 Provider Adapter”顺延为阶段 39。不要先做多 provider；先把产品主路径理顺。
- 先保持 MVP-0.3 / MVP-0.4 已完成链路稳定，持续跑 Mock / SQLite 回归验证
- 当前不要继续在旧手搓前端或 Node.js 原生 HTTP 后端上叠功能；旧原型归档方案已确认
- 阶段 31-35 已完成：gate、dry-run、文件范围锁、极小真实 Runner 执行、受控模型目录。34 仅临时沙箱路径+固定 Git 只读命令+人工二次确认；35 从后端受控模型目录选择模型。
- 旧原型先归档和参考，不要直接删除；等新架构覆盖对应能力后再迁移或清理
- 旧目录清理只能提出建议；实际删除必须人工确认、已有 Git 存档，并逐项说明回滚方式
- 按 `dev-docs/应用真正可用落地计划.md` 分阶段推进真实可用能力
- 真实模型调用已落地：阶段 25.3 写入脱敏 model_calls 审计记录，阶段 26 可手动保存为待审批草案；仍不写 runtime events
- 阶段 3 已收口；继续维护 Agent Run 记录链即可，不要把它升级成真实模型调用或真实 Runner 执行
- 不要绕过 Model Gateway 发 provider 请求；不要导入 provider SDK、读取 raw key 或返回 raw key
- 真实模型调用、Agent Run 真实链路和 Runner 真实执行必须拆开做，不要一次性打开
- Runner 已按阶段 34 边界开放（仅临时沙箱+固定 Git 只读命令+人工二次确认）；新增执行能力必须走独立阶段设计和审批，不得在模型调用链、Agent Run 记录链或执行计划审查链未稳定时扩大 Runner 范围
- 不要把 0.5 / 0.6 路线重新写回当前主线
- 每次完成一块就提交一个清晰 commit

## 给下一位 AI 的简短提示词

```text
你正在接手 F:\projects\agent-swarm。
先读 AGENTS.md、docs/Agent宪法.md、docs/AI开发细则.md、dev-docs/当前项目导航.md、dev-docs/下一步开发路线.md、dev-docs/新窗口交接说明.md。
当前进入重新立项后的工程初始化落地；技术栈、目录架构、共享 UI、Tauri/Rust 桌面宿主、SQLite 只读 commands、写入安全边界裁剪文档、写入 commands 设计、Rust 实现、正式验收、前端共享 UI 写入接入、真实 Tauri 桌面写入验收、旧原型归档方案、旧原型页面迁移清单、阶段 16 独立页面拆分、阶段 17 长期分层边界设计和阶段 18 共享类型与规则骨架已确认。`packages/shared` 和 `packages/agent-core` 已建纯 TypeScript 骨架；`packages/ui` 通过 tsconfig paths 从两个新包导入类型和规则；`apps/desktop` 已接通 6 个写入 commands -> SQLite 链路。当前阶段是 MVP-0.4 已验收、阶段 2 真实模型调用准入设计已收口、阶段 3 Agent Run 记录链已收口为本地 Mock / SQLite 流程。
真实 Runner 已按阶段 34 边界开放（仅临时沙箱+固定 Git 只读命令+人工二次确认，所有执行须经完整闸门链）；不开放批量/自动 Runner、自由命令、Git commit/push、文件删除、网络请求。真实模型已按阶段 35 边界开放（经 Model Gateway、受控模型目录、脱敏审计链路的人工二次确认 project_plan_generation）；不开放前端自由模型配置、key、base URL、prompt、header、body。阶段 35.1-35.4 前端中文化、主控台和项目计划工作台已收口。阶段 37 想法引导官已完成。下一步是阶段 38：项目类型分流与通用想法入口。不做云同步、完整权限系统。不要导入 provider SDK；不得读取或返回 raw key、raw prompt、raw response，不得把 raw provider error 写入前端或日志。
不要继续在旧手搓前端或 Node.js 原生 HTTP 后端上叠功能。阶段 23 model_calls helper-only 迁移已收口：migration 003 + helper-only 草案 + 70 个 Rust 测试通过。阶段 24 project_plan / Workflow 最小闭环迁移已完成：旧 MVP-0.3 的项目计划审批、5 个任务拆分和 5 条只读 Runner request 队列已迁入新 Tauri/Rust 主线。真实模型和 Runner 已按阶段 34/35 边界开放；不要超出当前边界扩大调用或执行范围。
```
