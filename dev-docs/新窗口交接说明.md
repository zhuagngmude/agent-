# agent蜂群新窗口交接说明

## 当前快照

- 项目：`F:\projects\agent-swarm`
- 当前阶段：MVP-0.3
- 当前最新 commit：`24f32c5 Complete MVP-0.3 project plan approval flow`
- 当前闭环：项目计划审批 -> Agent 自动分工 -> 只读 Runner request queue

## 这批已经做了什么

- 新增 `services/api/project-plan.js`
- 新增 `POST /api/projects/:projectId/project-plan-requests`
- 工作流页加入项目计划审批入口
- 审批通过后生成五个 queued 任务和五条 `runner_request_readonly` 队列记录
- Mock / SQLite flow、UI smoke、helper 验证都已同步

## 现在的边界

- 不开放真实 Runner 执行
- 不调用真实模型
- 不写本地项目文件
- 不改 Git
- 不做云同步
- 不做完整权限系统

## 受保护路径

- `design/image2/`
- `_internal/`
- `data/mock/runtime-state.json`
- `data/local/`
- `logs/`
- `.playwright-cli/`

## 先读什么

```text
AGENTS.md
docs/README.md
dev-docs/README.md
docs/mvp-0.3-project-plan-flow-spec.md
docs/demo-checklist.md
scripts/README.md
```

## 推荐继续做什么

- 继续完善项目计划审批相关的可视化和验收
- 如果要往前走，先做 Runner request review / dry-run 规格，不要直接接真实执行
- 每次完成一块就提交一个清晰 commit

## 给下一位 AI 的简短提示词

```text
你正在接手 F:\projects\agent-swarm。
先读 AGENTS.md、docs/README.md、dev-docs/README.md、dev-docs/AI开发维护手册.md、dev-docs/新窗口交接说明.md。
当前阶段是 MVP-0.3：项目计划审批 + Agent 自动分工 + 只读 Runner request queue。
不要开放真实 Runner、真实模型、云同步或完整权限系统。
优先沿着现有 Mock / SQLite / UI / docs / scripts 的边界继续。
```
