# 下一步开发路线

日期：2026-06-19

## 当前状态

当前主线已经从“只读演示/审批链路”转为“本地多模型 Agent 调度系统”。

当前准绳见：[当前产品目标与落地路线](./当前产品目标与落地路线.md)。

已具备的关键能力：

- 主控台可以作为入口发起全自动执行。
- Runner 已能产出第一个项目文件。
- 任务页已经开始向“总任务 + 子任务”的结构收敛。
- 系统设置已有模型服务配置入口。
- 输出目录统一到 `workspace/generated`。
- UI 主导航已扩展为主控台、任务拆解、流程蓝图、运行输出、AI 员工、审批与安全、系统设置。
- AI 员工页已接入第一批真实 Tauri commands，可读取执行器、模型目录、Agent 模板、项目成员、Skill 和边界检查记录；浏览器预览仍保留示例数据。

## P0 剩余 6 步（2026-06-19 已完成）

已完成的 6 个 checkpoint：

1. **AI 员工页收口** — 专家推荐走 `project_agents(source=recommended/manual)`，Tauri 模式不再写 localStorage。
2. **任务绑定真实项目 Agent** — `tasks.assigned_agent_id` 优先绑定 `project_agents.id`，旧 `agents` 表 ID 向后兼容；`ensure_agent_belongs_to_project` 全面覆盖 `project_agents`。
3. **总控确定性分派规则** — 新增 `recommend_project_agents`，确定性规则匹配核心员工和专家。
4. **Runner 执行前边界强校验** — `check_agent_boundary` 已接入 Runner preflight；真实项目 Agent 的 `denied` 会阻断 preflight，并写入 `agent_boundary_checks`。
5. **运行输出按 Agent 闭环** — 新增 `get_task_agent_info`，任务页已展示项目 Agent 名称、角色、执行器、模型和模块范围。
6. **验收和旧入口清理** — 文档同步更新，AI 员工页 Tauri 模式 localStorage 写入已移除。

后续可继续增强：
- AgentRunsPage 可进一步把历史 `agent_runs.agent_id` 与 `project_agents` 做关联展示。
- 专家模板 seed 可继续人工扩充，覆盖更多推荐场景。

## 近期优先级

### P0：多模型 Agent 调度主线（核心链路已贯通）

- ✅ 执行器配置、模型目录、员工池、项目专家已落到 SQLite (`executor_configs` / `model_catalog` / `agent_templates` / `project_agents` / `executor_skills`)。
- ✅ 总控确定性分派规则已接入 (`recommend_project_agents`)。
- ✅ 任务绑定 `project_agents.id`，后端全面校验。
- ✅ Runner preflight 已接入边界闸门 (`check_agent_boundary`)。
- ✅ 任务页可追踪 Agent/执行器/模型 (`get_task_agent_info`)。
- 下一步：让 AgentRunsPage 对历史运行记录补充项目 Agent 上下文。

### P0：职责边界和安全

- 固定员工和专家 Agent 只能在自己的职责模块内工作。
- 前端、后端、数据库、桌面端、安全、测试、文档等边界必须进入任务分派规则。
- 高风险动作继续进入审批；不能因为多 Agent 调度绕过 Runner 或审批。
- UI 选择模型不能直接修改真实执行配置，必须走受控配置和审批。
- 禁止 Agent 自由 shell、Git commit/push、文件删除、保护路径写入和未受控网络请求。
- 越界防护必须落到后端强校验：agent_id、task_type、module_scope、target_path、forbidden_actions 都要参与判定，不能只靠前端提示或 prompt。

### P1：产品体验

- 主控台应能展示总控如何选择角色和专家。
- AI 员工页应区分“已接入员工”和“待接入模板”。
- 项目成员视图应展示当前项目真正参与的 Agent。
- 运行输出页应展示每个 Agent 的输入、输出、模型、状态和产物。
- 用户可见文案默认中文，内部 ID 不直接裸露给用户。

### P1：模型体验

- 系统设置里清楚展示当前模型是否可用。
- key 过期、余额不足、Base URL 错误、模型 ID 错误要区分提示。
- 支持不同 Agent 绑定不同模型或执行器。
- 不得把 API Key、Token、私钥、raw prompt、raw response 写入文档、数据库或日志。

### P2：质量体检和旧入口清理

- 检查文档、页面、Rust service、Tauri command、共享类型是否一致。
- 补齐最小验证：UI typecheck、Rust check、关键 Runner 路径手测。
- 把历史问题单改成“历史记录 + 当前状态”，避免下一位智能体重复修已修问题。

## 不要做

- 不要把真实 API Key 写进任何文档、代码、日志或数据库。
- 不要做与“本地多模型 Agent 调度系统”无关的旁支功能。
- 不要绕开总控、职责边界、模型网关、Runner、审批和运行记录。
- 不要把历史“只读/必须人工确认”阶段边界当成当前产品限制。
- 不要碰 `design/image2/`、`data/local/`、`logs/`、`_internal/`。
- 不要擅自 Git push、Git commit 或删除用户文件。

## 推荐下一轮执行顺序

1. 清理入口文档、旧入口、误导性“待实现”文案和无真实功能的按钮。
2. 盘点 AgentRunsPage 当前数据来源，补齐项目 Agent、执行器、模型和产物上下文展示。
3. 审查并扩充 `agent_templates(category=expert)` seed，覆盖更多项目类型和技术栈。
4. 针对 Runner preflight + boundary check 增加更贴近主链路的回归测试。
5. 对 UI 主路径做一次手测：主控台 -> AI 员工 -> 任务拆解 -> 运行输出 -> 审批与安全。
6. 对疑似无用代码先用 `rg`、测试和编译确认引用；不能证明安全删除的内容只记录清理建议。

## 当前执行计划

- P0 6 个 checkpoint 已完成，后续不再按 [P0-AI员工模型执行器配置落地计划.md](./P0-AI员工模型执行器配置落地计划.md) 继续拆同一批任务；该文档保留为阶段背景。
- 当前执行重点是项目清理和 P1 增强：运行输出按 Agent 补全上下文、专家模板 seed 扩充、旧入口和旧文案收口。
- `workspace/generated` 下的既有删除记录保持不动，除非用户单独要求处理产物文件。
