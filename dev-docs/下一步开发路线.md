# 下一步开发路线

日期：2026-06-14

## 当前状态

- 阶段 25 “真实模型最小可用接入设计”已新增，见 `dev-docs/阶段25-真实模型最小可用接入设计.md`。阶段 25 只允许人工触发 `project_plan_generation` 真实模型预览；真实 Runner 继续后置，不执行命令、不写文件、不改 Git。
- 阶段 24 “project_plan / Workflow 最小闭环迁移”已完成，见 `dev-docs/阶段24-project-plan-workflow最小闭环迁移设计.md` 和 `docs/project-plan-workflow-acceptance.md`。旧 MVP-0.3 的项目计划审批、5 个任务拆分和 5 条只读 Runner request 队列已迁入新 Tauri/Rust 架构；仍不调用真实模型、不启用真实 Runner、不写用户项目文件。
- 阶段 23 “model_calls helper-only 迁移”已收口，见 `dev-docs/阶段23-model-calls-helper-only迁移设计.md`。已完成 `003_add_model_calls.sql` 18 字段审计表 + 3 索引、Rust helper-only 草案和验收测试；`feature_disabled` 返回不写 `model_calls`、不写 `runtime_events`，不调用真实 provider。
- MVP-0.4 已验收；阶段 2 真实模型调用准入设计已收口；阶段 3 Agent Run 记录链已收口为本地 Mock / SQLite 流程。
- 已完成 MVP-0.3 主线：项目想法 -> `project_plan` 审批 -> 5 个 queued 任务 -> 5 条只读 Runner request 记录。
- 已完成 MVP-0.4 主线：execution request 审查视图、生命周期流转和 runtime events 审计闭环。
- 阶段 2 已完成 helper-only scaffold：`model_calls` 结构草案、Model Gateway 未来正式入口、provider 配置边界、redaction helper、禁用态 route、helper-only 写入 / 迁移草案。
- 阶段 3 已完成本地记录链：`agent_runs` Mock / SQLite API、失败注入、runtime event 审计、Web UI 记录页和 Mock / SQLite / UI 验证。
- 旧实现使用原生 HTML / CSS / JavaScript 和 Node.js 原生 HTTP，用于 MVP 验证；它不作为后续正式工程架构继续扩展。
- 技术栈已确认：桌面端主入口，Tauri 2 + Rust + React + TypeScript + Vite + Ant Design + SQLite + rusqlite，不单独起 Node/Fastify/NestJS/FastAPI 后端。

## 老师反馈后的调整结论

本轮不再继续在旧手搓架构上叠功能。需要先重新确认项目工程方案，再决定如何初始化新工程和迁移旧原型价值。

已确认的调整方向：

- Agent 宪法、维护手册、交接说明和阶段路线默认使用中文。
- 前端不继续手搓，后续使用成熟 UI 技术栈。
- 桌面端是主入口，Web 只作为辅助预览或后续扩展入口。
- Web 和桌面端必须共用同一套 UI 源码。
- 桌面端使用 Rust/Tauri 作为宿主，Rust 负责本地能力，不重新手画 UI。
- 根目录重复前端、`apps/web` 前端、`packages/ui` 预留等目录需要重新规划，不能继续混乱叠加。
- 旧原型归档方案已确认：旧原型冻结为参考资产，不直接删除；等新架构覆盖后再讨论清理。
- 新项目目录架构已确认：`packages/ui` 为唯一 UI 源，`apps/desktop` 为主入口，`apps/web` 为辅助预览入口。
- `packages/ui` 已从预留 README 升级为 React + TypeScript + Vite + Ant Design 的最小可运行骨架。

## 当前最重要的边界

- 不写完整真实业务功能；当前只允许已验收的本地 SQLite 读链路和写入 commands 记录层。
- 不继续手搓前端。
- 不继续扩展 Node.js 原生 HTTP 后端。
- 不绕过已确认方案扩大桌面宿主、数据库和旧原型迁移范围。
- 不开放真实模型。
- 不开放真实 Runner。
- 不做云同步。
- 不做完整权限系统。
- 不修改保护路径：`design/image2/`、`data/mock/runtime-state.json`、`data/local/`、`logs/`、`.playwright-cli/`、`_internal/`。

## 新阶段列表

0. 重新立项讨论：进行中。
1. 中文治理文档：已完成第一版，后续按新架构继续修订。
2. 技术栈确认：已完成。
3. 新项目目录架构确认：已完成。
4. 共享 UI 方案确认：已完成。
5. Tauri/Rust 桌面宿主方案确认：已完成，并已初始化最小 Tauri 宿主。
6. 后端框架和数据库方案确认：已完成，SQLite 最小闭环已接入 `get_project`。
7. 旧原型归档方案确认：已完成，见 `dev-docs/旧原型归档方案.md`。
8. 新工程初始化：进行中，已完成 `packages/ui`、`apps/desktop`、SQLite 初始化、seed 读链路、`projects / agents / tasks / approvals` 只读 commands、`OverviewPage` 只读数据接入、Tauri 只读骨架正式验收文档，以及写入 commands 的设计、Rust 实现和正式验收。
9. 写入 commands 安全边界裁剪：已完成，见 `docs/write-commands-security-design.md`。
10. 写入 commands 设计：已完成，`create_task`、`update_task_status`、`create_approval`、`approve_approval`、`reject_approval`、`patch_only_approval` 均已设计，见 `docs/write-commands-design.md`。
11. 写入 commands Rust 实现：已完成，6 个 command 均已接入 Tauri invoke 并通过 Rust 测试。
12. 写入 commands 正式验收：已完成，见 `docs/write-commands-acceptance.md`。
13. 前端共享 UI 写入接入：已完成，见 `docs/frontend-write-commands-design.md`。`desktopHost.ts` 封装 5 个写入函数，`CreateTaskModal` 新建任务，`OverviewPage` 任务状态操作列和审批操作列均已接入。
14. Tauri 桌面写入交互验收：已完成，见 `docs/tauri-desktop-write-interaction-acceptance.md`。
15. 旧原型页面迁移清单：已完成，见 `dev-docs/旧原型页面迁移清单.md`。
16. 独立页面拆分：已完成，见 `dev-docs/阶段16-独立页面拆分方案.md`。OverviewPage 拆为 5 个独立页面，侧边栏导航扩展，数据提升至 App.tsx。
17. 长期分层边界设计：已完成，见 `dev-docs/阶段17-长期分层边界设计.md`。已确认六层边界、调用方向、旧模块冻结条件和阶段 18 最小实现范围。
18. 共享类型与规则骨架：已完成。`packages/shared` 和 `packages/agent-core` 已建纯 TypeScript 骨架，`packages/ui` 已通过 tsconfig paths 从两个新包导入类型和规则。
19. 冻结模块解冻评估：已完成，见 `dev-docs/阶段19-冻结模块解冻评估.md`。
20. Agent Run 记录视图只读迁移：已完成。Migration 002 + Rust 只读 command + TS 类型 + AgentRunsPage 均已实现，41 个 Rust 测试通过。
21. 真实模型接入新架构适配设计：已出，见 `dev-docs/阶段21-真实模型接入新架构适配设计.md`。
22. Model Gateway Rust helper-only scaffold：已完成。`services/model_gateway/` 四模块已建，`request_project_plan_model_draft` 已注册，61 个 Rust 测试通过。
23. model_calls helper-only 迁移：已收口。Migration 003、Rust helper-only 草案和 70 个 Rust 测试均已通过；不接前端、不新增 command、不调用真实 provider。
24. project_plan / Workflow 最小闭环迁移：已完成。Migration 004、`project_plan` Rust commands、共享类型、`ProjectPlanPage` 和验收测试均已落地；复刻旧 MVP-0.3 核心闭环但不执行 Runner。
25. 真实模型最小可用接入：设计已完成，见 `dev-docs/阶段25-真实模型最小可用接入设计.md`。实现前必须先批准 Rust HTTP 客户端依赖；第一版只做人触发真实模型草案预览，不落库、不创建任务、不启用 Runner。

## 候选技术栈口径

已确认方案，并已进入最小工程实现。

```text
桌面主入口：
Tauri 2 + Rust

前端 UI：
React + TypeScript + Vite + Ant Design

本地数据库：
SQLite + rusqlite

后续云端数据库：
PostgreSQL / Supabase，等单机本地版稳定后再讨论

共享 UI：
packages/ui 作为唯一 UI 源码
apps/desktop 作为主入口，apps/web 作为辅助预览入口
```

## 下一步应该做什么

1. 进入阶段 25.1：真实模型最小可用接入实现前置讨论，先确认 HTTP 客户端依赖方案（建议 `ureq`）和本地 env 配置方式。
2. 实现范围只允许：人工二次确认触发 `project_plan_generation`、调用 `openai_compat`、返回脱敏摘要预览。
3. 继续保持真实 Runner、Git 执行和用户项目文件写入关闭；Runner 后续必须等模型链、Agent Run 真实链和执行计划审查链稳定后再开。

## 仍然不要做的事

- 不要直接开始完整 React 业务重写。
- 不要直接删除旧前端目录。
- 不要继续在原生 HTML / CSS / JavaScript 上加新功能。
- 不要把真实模型调用、真实 Runner、云同步或完整权限系统混入重新立项阶段。
- 不要触碰保护路径。

## 参考

- [docs/Agent宪法.md](../docs/Agent宪法.md)
- [docs/README.md](../docs/README.md)
- [docs/tech-stack-notes.md](../docs/tech-stack-notes.md)
- [docs/api-draft.md](../docs/api-draft.md)
- [docs/data-model-draft.md](../docs/data-model-draft.md)
- [docs/demo-checklist.md](../docs/demo-checklist.md)
- [docs/tauri-readonly-skeleton-acceptance.md](../docs/tauri-readonly-skeleton-acceptance.md)
- [docs/write-commands-security-design.md](../docs/write-commands-security-design.md)
- [docs/write-commands-design.md](../docs/write-commands-design.md)
- [docs/write-commands-acceptance.md](../docs/write-commands-acceptance.md)
- [docs/frontend-write-commands-design.md](../docs/frontend-write-commands-design.md)
- [docs/tauri-desktop-write-interaction-acceptance.md](../docs/tauri-desktop-write-interaction-acceptance.md)
- [dev-docs/旧原型页面迁移清单.md](./旧原型页面迁移清单.md)
- [dev-docs/旧原型归档方案.md](./旧原型归档方案.md)
- [scripts/README.md](../scripts/README.md)
