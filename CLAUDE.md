# CLAUDE.md — agent-swarm

## 项目骨架

- 栈：Tauri 2 + Rust + React 19 + TypeScript + Vite + Ant Design
- UI 源：`packages/ui`
- 主入口：`apps/desktop`
- 预览入口：`apps/web`

## 当前事实

- 当前主线已打通：主控台输入目标 -> 自动生成任务 -> Runner 推进执行 -> 产物写入 `workspace/generated`
- 主要页面只保留：`主控台`、`任务拆解`、`系统设置`
- 模型配置在系统设置里完成，API Key 只进入当前桌面进程环境，不落库
- 用户可见文案默认中文

## 编码原则

1. 先看当前源码和文档，再动手。
2. 只改本轮需要的文件。
3. 小步验证，改完跑最相关检查。
4. 不改保护路径，不写密钥，不把假入口留给用户。

## 维护提醒

- 若改 API / 数据结构 / 阶段状态 / 主入口，请同步对应文档。
- 若新增用户能直接点到的入口，必须是真的能用，不是摆设。
